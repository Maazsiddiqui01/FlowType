<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- PyInstaller bundles unsigned third-party dylibs (ctranslate2, Qt, etc.); without
         this the hardened-runtime app crashes on launch. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- CPython / Qt / ctranslate2 need JIT + writable-executable memory. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Microphone capture. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- NOTE: App Sandbox is intentionally NOT enabled — it would block the global
         Input-Monitoring/Accessibility this app depends on. -->
</dict>
</plist>

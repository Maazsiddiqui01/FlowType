# Runtime dependencies — mirror of [project].dependencies in pyproject.toml.
# The packaged build and `pip install -e .` are the supported install paths; this
# file exists for `pip install -r requirements.txt` parity, so it MUST stay in sync.
faster-whisper>=1.1.1
# ctranslate2 is faster-whisper's native backend. Pin a floor so the bundled
# ctranslate2.dll / libiomp5md.dll / cudnn set cannot silently change under us.
ctranslate2>=4.7.1
httpx>=0.28.1
keyboard>=0.13.5
numpy>=1.26.4
Pillow>=10.4.0
PySide6>=6.7.0
pynput>=1.7.7
pyperclip>=1.9.0
pystray>=0.19.5
sounddevice>=0.5.1
